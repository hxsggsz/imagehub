import { ImageCard } from './image';
import { Root } from './root';
import { Content } from './content';
import { Menu } from './menu';

export const Card = {
  Root,
  Image: ImageCard,
  Content,
  Menu,
}